import CardBar from "./CardBar";
import UserCard from "./UserCard";

const Card = () => {
  return (
    <div className="py-8 border-b border-gray-300">
      <CardBar />
      <UserCard />
    </div>
  );
};

export default Card;
